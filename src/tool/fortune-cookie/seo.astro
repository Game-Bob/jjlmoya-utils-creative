---
import { SEOTitle, SEOTip, SEOArticle, SEOList } from '@jjlmoya/utils-shared';
import type { FortuneCookieLocaleContent } from './index';

interface Props {
  content: FortuneCookieLocaleContent;
}

const { content } = Astro.props;
if (!content) return null;

const { seo, faqTitle, faq } = content;
---

<SEOArticle>
  {seo.map((section: any) => {
    switch (section.type) {
      case 'title':
        return <SEOTitle title={section.text} level={section.level || 2} />;
      case 'paragraph':
        return <p set:html={section.html} />;
      case 'list':
        return <SEOList items={section.items} />;
      case 'tip':
        return <SEOTip title={section.title}><Fragment set:html={section.html} /></SEOTip>;
      default:
        return null;
    }
  })}

  {faq && faq.length > 0 && (
    <div class="fortune-cookie-faq">
      <SEOTitle title={faqTitle || 'FAQ'} level={2} />
      {faq.map((item) => (
        <div class="fortune-cookie-faq-item">
          <SEOTitle title={item.question} level={4} />
          <p>{item.answer}</p>
        </div>
      ))}
    </div>
  )}
</SEOArticle>

<style>
  .fortune-cookie-faq {
    margin-top: 4rem;
  }
  .fortune-cookie-faq-item {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background-color: #fffbeb;
    border-radius: 1rem;
    border: 1px solid #fef3c7;
  }
  .theme-dark .fortune-cookie-faq-item {
    background-color: #1e293b;
    border-color: #334155;
  }
</style>
